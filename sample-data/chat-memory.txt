Chat memory keeps the history of a conversation so the model can refer back to earlier messages. Because LLMs are stateless, every request must include the relevant previous messages. LangChain4j offers MessageWindowChatMemory, a sliding window limited by message count, and TokenWindowChatMemory, a sliding window limited by a token budget. The MemoryId annotation lets one assistant service manage separate memories per user or conversation.
