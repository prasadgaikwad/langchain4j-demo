Embeddings are dense vector representations of text that capture semantic meaning. Similar texts are mapped to vectors that are close together in the embedding space. Semantic search works by embedding a query and finding stored vectors with the highest cosine similarity. LangChain4j provides the EmbeddingModel abstraction and EmbeddingStore, including an in-memory store that can be persisted to JSON.
